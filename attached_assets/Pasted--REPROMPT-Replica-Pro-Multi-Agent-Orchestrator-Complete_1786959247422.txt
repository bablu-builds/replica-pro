# REPROMPT: Replica-Pro (Multi-Agent Orchestrator) - Complete Missing Components

## Project Context
I have a Python-based project named "Replica-Pro". It is a multi-agent orchestrator that takes a user's project idea, breaks it into parallel tasks using an LLM, and pushes code to GitHub branches/PRs. The existing codebase is clean, uses rmao CLI, and has mock mode working.

## Task
I need you to generate ALL the missing components listed below. Generate the complete, production-ready code for each. Do not skip any item.

---

### 1. Root README.md (Repository Overview)
- Location: Project root (/README.md)
- Must include:
  - Badges (Python version, License, CI status placeholder).
  - A 2-line description of the orchestrator.
  - Table of Contents.
  - Prerequisites (Python 3.9+, 4 Replit SIDs, GitHub Token, LLM API Key).
  - Quick Start guide covering installation, .env setup, and running in Mock Mode.
  - How to switch LLM providers (OpenAI/DeepSeek/Kimi/GLM).
  - Real Mode setup instructions.
  - Link to the detailed docs inside the replica-pro/ folder.

---

### 2. Replit Execution Provider (The Core Missing Feature)
- Location: replica-pro/src/rmao/providers/replit.py
- Since Replit doesn't have an official Python SDK for this automation, write a custom provider.
- Requirements:
  - Use aiohttp for async requests.
  - Authenticate using the connect.sid cookie (read from .env as ACCOUNT1_SID to ACCOUNT4_SID).
  - Write to Replit's internal GraphQL endpoint: https://replit.com/graphql.
  - Implement methods: create_repl(title, language), write_file(repl_id, path, content), run_command(repl_id, command).
  - Integrate this provider into the existing BaseProvider class (or adapt the current executor.py to use this).
  - Add error handling for rate limits (retry with backoff) and expired sessions.

---

### 3. Docker Setup (Containerization)
- Create a Dockerfile in the project root.
  - Use python:3.11-slim.
  - Copy replica-pro/ and install with pip install -e '.[dev]'.
  - Expose port 8000.
  - Command: rmao serve --host 0.0.0.0 --port 8000.
- Create a docker-compose.yml file.
  - Service app: builds from Dockerfile, mounts the code for hot-reload (if using development mode).
  - Service redis (optional) for caching and rate limiting.
  - Environment variables passed via env_file: .env.

---

### 4. CI/CD Pipeline (GitHub Actions)
- Location: .github/workflows/ci.yml
- Trigger on push and pull_request to main.
- Steps:
  - Checkout code.
  - Setup Python 3.11.
  - Install dependencies (pip install -e '.[dev]').
  - Run python -m pytest (ensure tests are present).
  - Run python -m compileall -q src for syntax check.
  - (Optional) Build the Docker image and push to GHCR if tests pass.

---

### 5. Web UI Dashboard (Monitoring)
- Location: replica-pro/src/rmao/ui/dashboard.py (or /ui folder).
- Use Streamlit for simplicity (since it's Python).
- Features required:
  - Display current running tasks and their status (Pending, Running, Completed, Failed).
  - View logs of the orchestrator in real-time (tail logs).
  - Show GitHub repo URLs generated by the orchestrator.
  - A simple form to trigger a new build manually from the UI (instead of CLI).
  - Authentication: Basic username/password (read from .env).

---

### 6. Webhook Support (Trigger via GitHub/External)
- Location: replica-pro/src/rmao/api/webhooks.py
- Add a new FastAPI route: POST /v1/webhooks/github.
- Accept JSON payloads.
- Support a custom X-Orchestrator-Token header for security (validate against .env).
- On receiving a valid payload (e.g., push event), parse the commit message or a specific file (like .rmao/config.json) to extract the project query, and trigger the orchestrator asynchronously (background task).
- Return a 202 Accepted response with a run_id immediately.

---